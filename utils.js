// This file can be used for utility functions if needed later.
// For example, vector math, complex collision detection, etc.

// Example:
// export function normalizeVector(vector) {
//     const length = Math.sqrt(vector.x * vector.x + vector.y * vector.y);
//     if (length === 0) return { x: 0, y: 0 };
//     return { x: vector.x / length, y: vector.y / length };
// }

// For now, it's empty as most simple utils are within classes or not complex enough yet.

